---
description: 多 Agent 进度/需求/WIP 总控；开干前必读 docs/project 并回写看板
alwaysApply: true
---

# 项目进度与协同（强制）

**唯一总入口**：[`docs/project/README.md`](docs/project/README.md)

## 开工前

1. 读 `docs/project/README.md` 一页总览（含双队列镜像）。  
2. 查 [`03-requirements.md`](docs/project/03-requirements.md) 与 [`05-wip-board.md`](docs/project/05-wip-board.md) **§0 本 Agent 队列**。  
3. 认领：仅写入 **本队列**（§0.A cursor / §0.B gemini）；校验与另一队列 **REQ+热点互斥**；主看板 Owner=`agent:<slug>` · `Doing`；03 → `in_progress`。  
4. 勿抢同一热点路径（见 [`06-process.md`](docs/project/06-process.md)）。

## 需求与变更

- 新需求/变更/拒绝 → 先写 03（`REQ`/`CHG`/`REJ`）。  
- 未立项隐患 → 04，成熟后 Promote 到 03。  
- 禁止另建平行总进度文档。  
- 旧路径 `docs/project-progress.md` 仅为跳转页。

## 收工

- 更新 05 进度（本开发队列出队）；完成则 03=`done`、刷新 02 Git 快照（必要时同步 04/README）。  
- 累计 Done≥阈值或专题包关闭 → 将批次写入**对方** §0.R（交叉审修），并简报 Human（`CHG-012`）。  
- 联审 → 07，落地必须进 03。  
- **自动 Git（默认）**：落地自检通过后独立 commit（带 REQ/CHG）并 `push origin HEAD`；禁夹带 `.env` / GEX HTML / `scratch`。生产 ff / C2 仍须 human。

## 红线

- 企微仅 C0 + `gex.collect`；C2 须 human HITL（禁止 Agent 代跑 `human-approve`）。  
- 不注册 `place_order`；告警不得调 C2；GEX 不在 GCP 拉链。  
- **禁止提交** `data/gex/*.html` / 大快照；`latest.json` **仅里程碑式**提交（禁盘中例行 commit）。  
- `catalog.yaml` 变更须独立 REQ/CHG。
