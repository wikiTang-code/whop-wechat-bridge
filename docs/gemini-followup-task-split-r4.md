# 后续任务拆分 Round 4（灰度前必须项 → 给 Gemini）

> 基线：`85f46ef` + Cursor 复核修订 Runbook / 行为差异表。  
> **禁止**：未关闭下列缺口前在 GCP 切双进程；禁止 `pm2 delete all`；禁止改生产 `ecosystem.config.cjs`（用 sample）。

## 阻断灰度（P0）

### T15 — 把 Auto News / Auto Persona 迁入 ingest tick

在 `executeIngestTick` 成功路径（非 dry-run）调用与 `server.js` 同等的：
- `checkAndAutoGenerateDailyNews`
- `checkAndAutoUpdatePersonaPlaybook`  
或证明由独立 cron 覆盖并改 Runbook。  
**验收**：单测 mock；行为差异文档更新为“已迁移”。

### T16 — web_runner 拉起 Cloudflare Tunnel（或显式弃用方案）

对齐设计“Tunnel 挂 web”；若暂不迁，Runbook 写清公网入口如何保持。  
**验收**：本地可开关 `ENABLE_TUNNEL=1`；默认 off。

### T17 — ingest 补 Supervisor + event-loop + AI tunnel 探针

monitoring 单写仍在 ingest；与单体能力对齐。  
**验收**：ingest 进程 `/` 无 HTTP 也可；心跳 + metric_samples 持续增长。

## 应做（P1）

### T18 — 只读 API 响应与现网 frontend 契约对齐

对照 `server.js` 的 `/api/messages` 等 JSON 形状，避免看板白屏；缺字段补齐或做适配层。

### T19 — Runbook / sample 联调检查清单（文档）

确认：停单体 → 起 sample → 403 写 → 回滚；含 `pm2 save`；禁止并行双写（已写入修订 Runbook，请回归测试文档步骤）。

## 顺序

**T15 → T16 / T17 并行 → T18 → T19**。全部本地通过后再提灰度申请。
