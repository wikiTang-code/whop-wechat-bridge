# data/gex

本目录由 `tools/gex-sidecar` 写入。入库的是第一版样例：

- `latest.json` — 嵌入用结构快照
- `heatseeker_gex.html` / `gex_matrix_TSLA.html` — 读图
- `snapshot_20260905_235029.json` — 与 latest 相同的带时间戳副本

之后新的 `snapshot_*.json` 默认不提交。
